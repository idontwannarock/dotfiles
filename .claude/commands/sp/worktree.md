---
name: "SP: Git Worktrees"
description: Use when starting feature work that needs isolation from current workspace
category: Workflow
tags: [superpowers, git, worktree]
---

Invoke the `superpowers:using-git-worktrees` skill using the **Skill tool**, passing through any arguments provided after the command.
