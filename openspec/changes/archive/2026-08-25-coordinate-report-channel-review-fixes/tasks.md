## 1. 邏輯與矛盾(先修:它們會讓覆核者做出錯誤判斷)

- [x] 1.1 **門檻理由方向改正**(D2):`coordinate.md` 的「為什麼是 5 行」改成「門檻**不得縮到低於**通知本身的長度」;`spec.md` 同一句 SHALL 同步。**5 行這個值不得改動**
- [x] 1.2 歸檔的 `design.md` **追加**一行更正註記指向本輪,**原句一字不動**(D1)
- [x] 1.3 **範圍標記進兩個 tool 分支**(D3):把「只管未升級的第一格」寫進 `{{ if }}` 與 `{{ else }}` 各自的句子,刪掉分支外那份
- [x] 1.4 **升級規則移除「括號」**(D4),並加一句說明觸發集與機制表對齊
  - 驗:`{{ if }}`/`{{ else }}` 兩端各自 render 後自讀一次,確認沒有「無條件適用」與「只管第一格」相鄰
  - 驗:觸發字元集與機制表逐項對照(列舉,不預測)

## 2. 規則失去承重的部分

- [x] 2.1 **第二格補回落點**(D5):表格那一列寫回 `attachments/`;〈一則一檔〉導向附錄 B 的句子限定為「第一格升級上來的與第三格的檔案」
- [x] 2.2 附錄 A 第三條修法改寫:「訊息寫進檔案再 `"$(cat file)"`」是**檔案當傳輸手段**,與本協定的**檔案當載體**相反。改為指向 `msgs/` 只送路徑
- [x] 2.3 **範圍標記斷言縮回真值**(D10):第三格的通知句仍在適用範圍內,並寫明升級規則救不了它
- [x] 2.4 **檔名約束字元集**(D9):`<topic>` 限 `[a-z0-9-]+`,理由寫明 `!53` 那個具體失敗
- [x] 2.5 **`create-only` 給機制或誠實化**(D10):寫入前檢查存在並中止;或把「結構上不存在」降級為帶前提的說法
- [x] 2.6 **通知與檔案定序**(D10):先寫檔 → 驗在 → 才通知,兩個方向的失敗各寫一句
  - 驗:逐條列舉本組六項,對照 body 實際文字分類
  - blocked by 1.3(範圍標記的落點要先定)

## 3. 覆蓋不足

- [x] 3.1 **拆艦隊 gate**(D7):新增「拆艦隊之前」清單,擁有者是協調者,涵蓋廣播、合併後跨線事實、**每一則裁決**,以及**意外死亡的線**留下的訊息
- [x] 3.2 **收尾項⑥ 改歸「只能問」並補「不適用」分支**(D8);同時把 `:597`「那兩項」改為內容命名(上一輪掃漏的同族)
- [x] 3.3 **citation 義務改無條件 ＋ carve-out**(D6):「下一則回報一律引用,並說明是否照做;不照做要說為什麼」;第一格的一行話裁決不要求引用不存在的檔名。**兩份 body 同步**
- [x] 3.4 **上一輪的買單項改寫為已解決**:D4(上輪)那筆「分不出沒開檔／開了不動作」因 3.3 而消失,body 要明說,否則下游會繼續引用一筆不存在的代價
- [x] 3.5 **〈接手後第一件事〉加「讀 `msgs/`」**(D11)
- [x] 3.6 **殘留目錄警告補 `msgs/`**(body ＋ spec 的〈目錄名可重用〉scenario)
- [x] 3.7 **線側契約補檔名慣例與 `msgs/` 生命週期**:線只拿到目錄路徑產不出合規檔名;不知道目錄會死就無從判斷該搬什麼
  - 驗:三個 body 位置(coordinate 的線側清單、dev-workflow、派線酬載清單)說法一致
  - blocked by 3.3

## 4. 驗證(刻意不重複上一輪的方法)

- [x] 4.1 三支 shell 測試 × `sh`／`bash` 全綠
- [x] 4.2 `openspec validate --all` 與 `--changes` 全過
- [x] 4.3 **逐條列舉** 18 條 scenario,對照 body 實際文字分類。⚠️ 上一輪 14/14 全綠而本輪找出 12 項缺陷——**列舉法證明的是「有對應文字」,不是「那段文字說得對」**
- [ ] 4.4 **`code:review-cross-model`**(上一輪未跑)。這是本輪唯一能抓「同一批先驗的共同盲點」的關卡,**列舉法結構上抓不到那一族**
- [x] 4.5 `chezmoi diff` 限縮到本次子樹、逐行歸因刪除行、確認無退版後再 `chezmoi apply` 同一組路徑
  - blocked by 1-3 全部
