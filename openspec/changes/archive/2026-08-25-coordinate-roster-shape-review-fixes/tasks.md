## 1. 名冊寫入時機:補回協調者那一列(E1)

`home/.chezmoitemplates/skills/coordinate.md`

- 「寫入只有兩個時機」→ **三個**:派線(增線的一列)／開艦隊(建協調者那一列)／收尾(刪一列)
- 立論改成「**寫入者只有協調者一個**」,並明說「時機只有兩個」是手段不是理由
- 差集表補一句:協調者自己也在名冊上,否則第三列會把它判成外人
- 附錄 B 開艦隊那節補上「建立協調者那一列」;`:1215` 改成「**開接班協調者不重寫那一列**」(而非「派線時就寫好了」——對協調者為假)
- `:856` 的「派線那一刻」改為涵蓋兩種建立時機

**驗收**:全檔搜尋「寫入」與「時機」,不得再有任何一處宣稱只有兩個;附錄 B 開艦隊段落存在建立協調者列的指令。

## 2. 自檢處置依角色分流(E2)

- 〈接手後第一件事〉第 2 項的處置拆成兩列:線 → 具名回報協調者;協調者 → **立刻對使用者說,並請使用者重開一個沒帶旗標的 session**
- 兩個半邊那張表同步分流
- 新增「名冊上沒有我這一列」分支:**這本身就是 finding**,要回報,不是「沒事可查」

**驗收**:協調者那一列的處置與 `:877` 既有處置逐字一致;搜尋「回報協調者」不得出現在協調者自己的處置上。**blocked by #1**

## 3. D2 縮成指路(E3)

- `:146` 改為:前任在交接文件寫**一句指路**——「接班者 SHALL 以名冊那一列的形狀開出來」——不複製值
- 刪掉「前任是唯一知道自己 argv 的人」這個已失效的立論
- `:1219` 那段的矛盾隨之消失,改寫成單向指路

**驗收**:全檔搜尋「argv」,不得再有任何一處用它替 D2 立論;`:1219` 與 `:146` 不再相斥。**blocked by #1**

## 4. `:204` 改成命名兩個半邊(E4)

- 刪掉前者／後者與套套邏輯,直接寫「指路是預防、自檢是偵測」
- 補上少了任一邊的後果

**驗收**:該段不含「前者」「後者」;不含「防『前任沒交代』」這類自指。**blocked by #3**

## 5. 「不可事後稽核」改寫成因(E5)

- 立論從「第三方不知道角色」改成「**herdr 的名字與 process 之間沒有可靠的 join**」
- 明說舊立論已被名冊本身推翻
- 禁令綁在 join 這個成因上,寫明 join 出現時它應失效

**驗收**:全檔搜尋「稽核」,每一處的理由都是 join,不得再有「不知道角色」版本。**blocked by #1**

## 6. 線側義務補投遞路徑(E6)

- 自檢義務加進派線訊息酬載那一節(與位址、升級契約同一則)
- 同步進 `home/.chezmoitemplates/skills/dev-workflow.md`〈coordinated mode〉的線側契約
- 立論改用「沒有機制讓線載入本 skill」,不用「線不讀你的 handoff」

**驗收**:兩份檔案都有該義務且說法一致(人工比對,機器驗不出);`sh tests/skill-name-map-axis.test.sh` 綠。**blocked by #2**

## 7. 三處事實更正 ＋ 兩處 hedge(E7)

- `:140` 統計改指 `:638`–`:658` 那一節的正確標題
- `context/principles.md`:`ps` 三態表一致的是「分不分得出」欄;窄版主張只涵蓋兩個未帶旗標的態
- **S2**:A/B 推論加 hedge(兩組都是無工具的,而少了旗標的線是有工具的)
- **S3**:旗標可行性依 kind 顯性退化,不得靜默通過

**驗收**:`:140` 指到的標題確實含那個統計;`principles.md` 那句與三態表逐欄對得上。

## 8. 更正前一輪的記錄(E8)

`openspec/changes/archive/2026-08-25-coordinate-roster-shape-on-handover/`

- `tasks.md` task 5 驗收改成真正的不變量:**沒有任何一處指示寫入名冊**(禁令是合格的),並附上五處分類
- 移除 tasks 2–5 對 `skill-name-map-axis.test.sh` 的引用,或改註明它對該 diff 零涵蓋
- `design.md` Migration Plan 的「自然收斂」改成「舊列讀得懂即可」
- `design.md` 裡的〈共寫檔〉統計引用一併更正

**驗收**:三處各自貼出改後文字。

## 9. 收尾驗證

- `openspec validate --all` 全過
- 三支測試在 `sh` 與 `bash` 下各跑一輪全綠
- `chezmoi diff` 限縮到本次子樹(coordinate ＋ dev-workflow,共四個目標)確認後 apply,再 diff 驗空

**驗收**:三項各自貼出輸出。**blocked by #1..#8**
