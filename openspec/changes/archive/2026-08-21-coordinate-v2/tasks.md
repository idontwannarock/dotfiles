## 1. 主體新增三節

- [x] 1.1 〈證據的資格〉：四層表 ＋ 紅燈歸因 ＋ 失敗計數 ＋ 守衛即探針規格 ＋「答案屬於哪一個時刻」
- [x] 1.2 〈map 是線與線之間的那一格〉：兩格性質相反、資源格三欄、寫入權表、三載體分工、rebase 後強制更新
- [x] 1.3 〈誰有資格說這句話〉：該由誰說／轉述第三方／連轉述都不夠（含代價與解法）／選單回了沒被選過的答案
- 驗證：`grep -c '^## '` 增加 3；三節各自第一句話點名它防的是哪一種失效

## 2. 主體既有節擴充

- [x] 2.1 〈不要照單全收下游的結論〉：後任 ← 前任、否定斷言與合併佇列、先確認讀的是能回答它的東西
- [x] 2.2 〈跨線事實〉：量的還是記的、協調者不得以推論回答查得到的問題
- [x] 2.3 〈整份覆寫之前先讀〉：條件寫三層、`assert` 的保護範圍、成品讀回來、症狀是空白不是錯字
- [x] 2.4 〈共寫檔〉：checkout 是共寫資源、工具讀取設定的位置、`str.replace()` 靜默 no-op
- [x] 2.5 〈認得複寫了別處的事實〉：四處加強 ＋ 測試站在錯的那一邊
- [x] 2.6 〈收尾〉：第②項可判「不適用」、第六項「還有什麼只有你知道」、小坑要有住址、覆核者的資格
- [x] 2.7 〈交付物不要留在會消失的地方〉：訊息 vs 檔案
- [x] 2.8 〈排程〉：狀態條件不要寫成時序條件
- [x] 2.9 〈派線時關掉線的發問管道〉：加一句指回〈誰有資格說這句話〉（避免與 #39 咬死）
- [x] 2.10 〈反模式〉：傳了規則沒傳前提、統一處理前先問現況為何不統一、規則的來源鏈就是適用範圍
- 驗證：每條新增內容都能指回 backlog 的編號；`openspec validate --changes coordinate-v2 --strict` 通過

## 3. 附錄擴充

- [x] 3.1 附錄 A：dim placeholder 機械檢查置於「是誰留的」判斷表**之前**
- [x] 3.2 附錄 A：傳訊兩層表（免疫管道／退路），`agent prompt` 那節保留並降級為退路說明書
- [x] 3.3 附錄 A：三種吃字機制表 ＋ 完整修法 ＋ 「掃描要找空白」
- [x] 3.4 附錄 A：idle pane 的畫面讀取會過期，按鍵有效讀取無效
- [x] 3.5 **改放主體**〈整份覆寫〉的「工具回報成功不等於做了事」子節：`--abort` 是失敗路徑的還原，不是成功路徑的（判準：git 行為換 repo／換 merge 平台／換 agent kind 都不會失效，屬主體）
- 驗證：Codex 版 render 後不得出現 Claude 專屬旗標

## 4. 部署與驗證

- [x] 4.1 四份 template render 通過（Claude／Codex × coordinate／dev-workflow）
- [x] 4.2 限縮 `chezmoi apply --source <worktree>/home` 並點名 target **目錄**
- [x] 4.3 `chezmoi status` 對四個 target 乾淨
- [x] 4.4 `openspec validate --specs --strict` 全過
- [ ] 4.5 開 PR

## 5. 覆核

- [ ] 5.1 請 coordinator review 事實正確性，並就 D10（四則收成一族）明確表態
