## 1. 載體判準:三格與升級規則

- [x] 1.1 改寫 `coordinate.md`〈交付物不要留在會消失的地方〉末段那張「訊息 vs 檔案」表為**三格**(D1),第三格明寫「兩個都要,不是二選一」;承重規則置頂(該節首段判準:讀者只讀首段會不會做出正確動作)
- [x] 1.2 在同一節緊接著加**一條升級規則兩個觸發條件**(D5):含反引號／`$`／`!`／括號,或超過 5 行或 500 字元 → 升第三格。寫明雙上限的理由(單行數漏掉「一行三千字」)、5 行的來由(通知本身的長度),以及「守衛不是告誡」與吃字實測敗績
- [x] 1.3 立論段落明寫**省 context 經不起推敲**,三個硬理由(可重讀／吃字整族消失／事情發生過的證據)置於承重位置
- [x] 1.4 〈跨線事實〉一節加一句把該節產出**明文歸入第三格**,並指回 1.1 那張表,SHALL NOT 在該處複寫判準(避免〈複寫了別處的事實〉)
  - 驗:`grep -n` 確認判準表只出現一處;三格與升級規則在同一節內

## 2. 落點、檔案佈局與收尾

- [x] 2.1 附錄 B 落點段落加 `msgs/` 子目錄(D2),並加一句說明線間訊息與名冊、map 同生命週期
- [x] 2.2 寫明**一則一檔、動作是建檔**(D3)與檔名形狀 `<ts>-<from>-<to>-<topic>.md`,理由寫「讓〈整份覆寫之前先讀〉那一族在結構上不存在」,並指出收訊方沒有「讀到哪了」這個狀態
- [x] 2.3 寫明**為何不落 `attachments/`**:綁 repo 不綁艦隊、無淘汰機制、量級遠大於交付物,淹沒會讓「可重讀」變成「找不到」
- [x] 2.4 〈收尾〉那份清單新增一項:**把該活過艦隊的搬出去**,標明失敗是靜默的,並要求引用時用**搬移後**的路徑
  - 驗:收尾清單該項落在「你自己驗」那半;`grep -c 'attachments'` 確認舊落點的敘述沒有被誤刪(它仍管協調者交付物)
- [x] 2.5 **實作揭露的既有缺陷**:〈收尾〉節本來就以序號承重(標題「七項」、「前五項」、「第②項」、「⑥ 編號回收」…),而既有 spec 條文〈收尾要問「還有什麼只有你知道」〉明文禁止此事,並預言「插入一項會讓所有指涉同時變假且沒有東西轉紅」。2.4 的插入正好引爆它,故一併改為內容命名(9 處)並在表上加一句禁令。**這是本次改動製造的孤兒,屬清理義務,非擴大範圍。**
  - blocked by 1.1(第三格的定義要先在)

## 3. 回執與退路範圍

- [x] 3.1 加**不設回執**那段(D4):查產物不要問它,理由引〈證據的資格〉與「你是量的還是記的」;裁決類加掛「下一則回報引用檔名」,寫明它的價值是「引用錯會露出來」而非「知道他讀了」
- [x] 3.2 明寫買單項:協調者分不出「沒開檔」與「開了不動作」
- [x] 3.3 附錄 A〈訊息內容會經過一層 shell〉開頭加一句**範圍標記**(D6):它現在只管未升級的純訊息那一格;同段寫明**兩個觸發條件都可能漏判**,漏判之後唯一還在守的就是這一節——**該節不得刪除**
  - 驗:人讀確認附錄 A 三個吃字機制的表格與修法**一字未刪**,只多了範圍標記

## 4. 線側契約(送達得到線的那一半)

- [x] 4.1 `dev-workflow.md` 的 `## When this line is one of several (coordinated mode)` 新增本協定的線側義務:線發回報時同樣適用三格判準與升級規則,裁決類回報要引用來訊檔名
- [x] 4.2 明寫線**推導不出** `msgs/` 的完整路徑(`<repo-slug>` 與 `<fleet>` 都不在它手上),故該路徑 SHALL 隨派線訊息的酬載送達——與 `fleet.md` 路徑同一則
- [x] 4.3 回頭在 `coordinate.md`〈與 dev-workflow 的關係〉一節,把本協定加進「凡是你要課給線的義務都得走這條路送達」那份清單
  - 驗:兩份 body 對同一義務的說法一致(人讀);`grep -n 'msgs/' home/.chezmoitemplates/skills/dev-workflow.md` 命中
  - blocked by 1.1, 1.2, 3.1

## 5. 驗證與部署

- [x] 5.1 三支 shell 測試在 `sh` 與 `bash` 下各跑一次全綠:`tests/skill-name-map-axis.test.sh`、`tests/path-format-flag-order.test.sh`、`tests/claude-memory-seed.test.sh`
- [x] 5.2 `openspec validate --all` 全過
- [x] 5.3 **逐一列舉**本 change 的每一條 spec scenario,對照 body 實際文字分類為「已寫入／未寫入」,**不得用預測式 grep 代替列舉**(前一輪的教訓:預測式 grep 在「我漏改了」時必然沉默,曾讓兩個沒做的子任務被驗成完成)
- [x] 5.3b **列舉結果**:14 條 scenario 中 13 條已寫入並有行號證據;**第 10 條「map 站在 workflow 之上」未寫入**——body 只有「一條線自己的狀態指回 `active_workflows.md`」,缺「收進去是降一層且會使目錄名說謊」。**這是既有落差,非本 change 造成**(該 scenario 依 MODIFIED 規則原文抄入)。回報後使用者裁決**納入本輪**,已於 map 那節補上「降一層＋目錄名說謊」一段。**14/14 全數寫入。**
- [x] 5.4 `chezmoi diff` **限縮到本次子樹**(`home/.chezmoitemplates/skills/coordinate.md`、`home/.chezmoitemplates/skills/dev-workflow.md` 及其 wrapper 產物),確認差異只有本次改動後再 `chezmoi apply` 同一組路徑
  - ⚠️ 本機常比 repo 新,全量 apply 會退版;多 target 依字典序執行且失敗即中止其後全部
  - blocked by 1-4 全部
