## 1. spec：修正名冊 requirement ＋ 新增交接形狀 requirement

改 `openspec/specs/discipline-skills/spec.md`——由 `openspec-archive-change` 於歸檔時套用 delta,本任務只確保 delta 本身自洽可套。

- 欄位清單:`開線旗標與角色` → `啟動形狀(kind ＋ 角色 ＋ 該不該帶旗標)`,並明文 `SHALL NOT 記 cmdline 字串`
- 新增 scenario〈記啟動形狀,不記 cmdline 字串〉
- 改寫 scenario〈寫入時機限於派線與收尾〉的立論:「名字沒變」→「形狀不得變」
- 新增 requirement〈coordinate 交接的啟動形狀不得改變〉共四個 scenario

**驗收**:`openspec validate --all` 全過;delta 內〈欄位清單〉與〈寫入時機〉兩處不再相斥——具體檢查是「把最後一個 scenario 的理由拿掉之後,欄位清單能不能單獨推出它」,能推出才算不相斥。

## 2. body：名冊段落改記啟動形狀

改 `home/.chezmoitemplates/skills/coordinate.md`(`:88`～`:118` 一帶)。

- 「記什麼」表格:`開線旗標與角色` → `啟動形狀`;右欄「不記」加入 `cmdline 字串`
- 差集判讀表第二列:`工作樹 ＋ kind ＋ 旗標` → `工作樹 ＋ 啟動形狀`
- 「寫入只有兩個時機」那段:立論從「名字不變」改為「形狀不得變」,並補一句「換形狀 ＝ 收線 ＋ 重新派線」
- 〈鏡子與帳本〉那句「交接時只有後者變」保留,但要與新立論接得上

**驗收**:`sh tests/skill-name-map-axis.test.sh` 綠;人讀確認 spec 與 body 說法一致(這一項機器驗不出來,見 design〈Risks〉)。**blocked by #1**

## 3. body：新增〈交接必須同形狀〉規則 ＋ 前任交代義務

同一份 body,加在名冊段落之後(或〈接手後第一件事〉之前——條件與例外要寫在它所限制的指令之前)。

- 交接 ＝ 照名冊那一列原地重開,形狀不得變;換形狀走收線 ＋ 重新派線
- 前任 SHALL 在讓名前把 kind ＋ 該不該帶旗標寫進交接文件
- 立論用「只約束得到現任」那個既有形狀,並指出前任是唯一知道自己 argv 的人

**驗收**:同 #2;且該段**排在**它所限制的指令之前。**blocked by #1**

## 4. body：〈接手後第一件事〉擴到線那一側 ＋ 自檢措辭

`:130` 那三項目前只課給協調者,第 2 項是查自己 cmdline 有沒有被誤帶旗標。

- 明示這道自檢**線與協調者都要做**,比對對象是名冊那一列的形狀
- 加入兩個半邊的區分表(旗標補不回來／契約可重發),並援引 2026-08-21 A/B 實測
- 措辭必須是**偵測並回報**;明文 SHALL NOT 自行繞過或改設定檔
- 加一句「這道取代不了前任交代那道」,並寫明兩者防的失效不同

**驗收**:同 #2;段落裡不得出現任何允許自行代償的措辭。**blocked by #1**

## 5. body：附錄 B 拔掉交接時的名冊寫入

`:1146` 一帶——「開完接班協調者之後把 `cmdline` ＋ 角色寫進 `fleet.md` 那一列」是目前唯一違反〈兩個寫入時機〉的地方。

- 改為:名冊那一列記的是**形狀**,在**派線那一刻**寫入;開接班協調者之後**不寫名冊**
- 保留「兩道指令不是一道」那條(它與本輪無關且仍然成立)
- 移除「`cmdline` 是唯一硬證據」那段立論,改為 design D3 的理由(它稽核不出東西)

**驗收**:全檔搜尋 `cmdline`,剩下的每一處都必須是**自檢**用途(讀自己的),不得有任何一處是**寫入名冊**用途。**blocked by #1**

## 6. 收尾驗證

- `openspec validate --all` 37 items 全過
- 三支測試在 `sh` 與 `bash` 下各跑一輪全綠
- `chezmoi diff` 限縮到本次子樹確認變更符合預期,再 `chezmoi apply` 同一子樹,然後 `chezmoi diff` 驗空

**驗收**:上述三項各自貼出輸出。**blocked by #2, #3, #4, #5**
