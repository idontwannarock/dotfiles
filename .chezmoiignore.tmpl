# ── Windows 專屬（非 Windows 環境排除）─────────────────────────────────────
{{- if ne .chezmoi.os "windows" }}
Documents
{{- end }}

# ── Unix shell 設定（Windows 排除）─────────────────────────────────────────
{{- if eq .chezmoi.os "windows" }}
dot_shell_common
dot_bashrc.tmpl
dot_bash_aliases
dot_zshrc
{{- end }}

# ── zsh 設定（只部署到 macOS）───────────────────────────────────────────────
{{- if ne .chezmoi.os "darwin" }}
dot_zshrc
{{- end }}

# ── bash 設定（只部署到 Linux/WSL）─────────────────────────────────────────
{{- if ne .chezmoi.os "linux" }}
dot_bashrc.tmpl
dot_bash_aliases
{{- end }}
