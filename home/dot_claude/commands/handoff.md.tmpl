---
description: Save current session state to a handoff file under ~/.agent/handoffs/. The file ends with the exact /pickup <id> command to revive it later. Pass --repo <path|name> to hand off to a different repo. Manual trigger only.
disable-model-invocation: true
---

{{ template "skills/handoff.md" . -}}
