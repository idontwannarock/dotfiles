{{- /* Claude name-map for the shared handoff body */ -}}
{{- $n := dict
    "memoryDir" "~/.agent/memory"
    "cli" "claude" -}}
---
description: Save current session state to a handoff file under ~/.agent/handoffs/. The file ends with the exact /pickup <id> command to revive it later. Pass --repo <path> to hand off to a different repo.
---

{{ template "skills/handoff.md" (dict "n" $n "chez" .chezmoi) -}}
